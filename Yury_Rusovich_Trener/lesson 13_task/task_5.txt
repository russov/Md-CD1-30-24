1) Используя Google test (gtest) создать юнит-тесты для тестирования std::string.
   Протестировать следующее:
   a) конструкторы
   b) push_back()
   c) size()
   d) find()
   e) и еще 3 любых метода на свое усмотрение
2) Написать свой класс string к созданным ранее тестам, чтобы все тесты выполнялись.
В ходе реализации необходимо использовать пройденный материал: классы, конструкторы/деструкторы, инкапсуляция, сетеры/гетеры, массивы(можно через умный указатель), 
методы с параметрами, передаваемые по ссылке/значению/указателю и так далее.
НО очень важно, чтобы ваша строка была безопасной, она может не иметь каких-то методов, но она не должна дорускать exeptions или утечки памяти
подсказка) Обратите внимание на копирование вашей строки, чтобы не получилось поверхостное копирование с указателями! 

*a) При необходимости можно реализовать дополнительные методы, чтобы ваша строка была более была удобная в использовании, 
но в случае реализации доп методов, необходимо для них разработать и тесты, чтобы код был на 100 процентов покрыт тестами.
*b) Реализовать сортировку(чтоб можно было через std::sort посортировать строку).
